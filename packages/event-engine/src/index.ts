/**
 * @mandate/event-engine —— 事件引擎。
 *
 * 职责：固定历史事件、条件事件（DSL 表达式）、动态事件、事件链推进、
 * 连锁触发深度上限（架构文档第 7 节）。
 * 红线：本包禁止依赖 llm-adapters / agent-runtime。
 *
 * 实现阶段：Phase 6。Phase 0 仅预留包位置。
 */
export const PLANNED_PHASE = 6;
